# THIS FILE IS AUTOGENERATED BY A CCL TOOL. DO NOT EDIT.

@obfuscate
@obfuscate_path
class_name LumenfallWorldState

const MODEL_ID_LUMENFALL_WORLD_STATE = 3

var schema_version: int
var world_id: String
var display_name: String
var played_seconds: float
var player_x: float
var player_y: float
var player_z: float
var player_yaw: float
var quest_stage: int
var active_quest_id: int
var completed_quest_ids: Array[int]
var inventory_item_ids: Array[int]
var inventory_item_counts: Array[int]
var unlocked_recipe_ids: Array[int]
var gold_coins: int
var riftglass_pieces: int
var current_health: float
var maximum_health: float
var last_played_unix: int
var tutorial_stage: int

func get_model_id() -> int:
	return MODEL_ID_LUMENFALL_WORLD_STATE

func clone_empty() -> LumenfallWorldState:
	return LumenfallWorldState.new()


func deep_clone() -> LumenfallWorldState:
	var clone := LumenfallWorldState.new()
	clone.schema_version = schema_version
	clone.world_id = world_id
	clone.display_name = display_name
	clone.played_seconds = played_seconds
	clone.player_x = player_x
	clone.player_y = player_y
	clone.player_z = player_z
	clone.player_yaw = player_yaw
	clone.quest_stage = quest_stage
	clone.active_quest_id = active_quest_id
	clone.completed_quest_ids = completed_quest_ids.duplicate()
	clone.inventory_item_ids = inventory_item_ids.duplicate()
	clone.inventory_item_counts = inventory_item_counts.duplicate()
	clone.unlocked_recipe_ids = unlocked_recipe_ids.duplicate()
	clone.gold_coins = gold_coins
	clone.riftglass_pieces = riftglass_pieces
	clone.current_health = current_health
	clone.maximum_health = maximum_health
	clone.last_played_unix = last_played_unix
	clone.tutorial_stage = tutorial_stage
	return clone

func serialize_binary() -> PackedByteArray:
	var buffer := StreamPeerBuffer.new()
	buffer.big_endian = false

	buffer.put_32(self.schema_version)

	var world_id_bytes = self.world_id.to_utf8_buffer()
	buffer.put_u32(world_id_bytes.size())
	buffer.put_data(world_id_bytes)

	var display_name_bytes = self.display_name.to_utf8_buffer()
	buffer.put_u32(display_name_bytes.size())
	buffer.put_data(display_name_bytes)

	buffer.put_float(self.played_seconds)

	buffer.put_float(self.player_x)

	buffer.put_float(self.player_y)

	buffer.put_float(self.player_z)

	buffer.put_float(self.player_yaw)

	buffer.put_32(self.quest_stage)

	buffer.put_32(self.active_quest_id)

	buffer.put_u32(self.completed_quest_ids.size())
	for item in self.completed_quest_ids:
		buffer.put_32(item)

	buffer.put_u32(self.inventory_item_ids.size())
	for item in self.inventory_item_ids:
		buffer.put_32(item)

	buffer.put_u32(self.inventory_item_counts.size())
	for item in self.inventory_item_counts:
		buffer.put_32(item)

	buffer.put_u32(self.unlocked_recipe_ids.size())
	for item in self.unlocked_recipe_ids:
		buffer.put_32(item)

	buffer.put_32(self.gold_coins)

	buffer.put_32(self.riftglass_pieces)

	buffer.put_float(self.current_health)

	buffer.put_float(self.maximum_health)

	buffer.put_64(self.last_played_unix)

	buffer.put_32(self.tutorial_stage)

	return buffer.data_array

static func deserialize_binary(data: PackedByteArray) -> LumenfallWorldState:
	var model_result := LumenfallWorldState.new()
	if not data or data.is_empty():
		return model_result

	var buffer := StreamPeerBuffer.new()
	buffer.big_endian = false
	buffer.data_array = data

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.schema_version = buffer.get_32()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	var world_id_len := buffer.get_u32()
	if world_id_len > buffer.get_size() - buffer.get_position():
		return model_result
	model_result.world_id = buffer.get_data_bytes(world_id_len).get_string_from_utf8()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	var display_name_len := buffer.get_u32()
	if display_name_len > buffer.get_size() - buffer.get_position():
		return model_result
	model_result.display_name = buffer.get_data_bytes(display_name_len).get_string_from_utf8()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.played_seconds = buffer.get_float()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.player_x = buffer.get_float()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.player_y = buffer.get_float()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.player_z = buffer.get_float()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.player_yaw = buffer.get_float()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.quest_stage = buffer.get_32()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.active_quest_id = buffer.get_32()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	var completed_quest_ids_len := buffer.get_u32()
	model_result.completed_quest_ids = [] as Array[int]
	for i in range(completed_quest_ids_len):
		if buffer.get_size() - buffer.get_position() < 4:
			return model_result
		model_result.completed_quest_ids.append(buffer.get_32())

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	var inventory_item_ids_len := buffer.get_u32()
	model_result.inventory_item_ids = [] as Array[int]
	for i in range(inventory_item_ids_len):
		if buffer.get_size() - buffer.get_position() < 4:
			return model_result
		model_result.inventory_item_ids.append(buffer.get_32())

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	var inventory_item_counts_len := buffer.get_u32()
	model_result.inventory_item_counts = [] as Array[int]
	for i in range(inventory_item_counts_len):
		if buffer.get_size() - buffer.get_position() < 4:
			return model_result
		model_result.inventory_item_counts.append(buffer.get_32())

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	var unlocked_recipe_ids_len := buffer.get_u32()
	model_result.unlocked_recipe_ids = [] as Array[int]
	for i in range(unlocked_recipe_ids_len):
		if buffer.get_size() - buffer.get_position() < 4:
			return model_result
		model_result.unlocked_recipe_ids.append(buffer.get_32())

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.gold_coins = buffer.get_32()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.riftglass_pieces = buffer.get_32()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.current_health = buffer.get_float()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.maximum_health = buffer.get_float()

	if buffer.get_size() - buffer.get_position() < 8:
		return model_result
	model_result.last_played_unix = buffer.get_64()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.tutorial_stage = buffer.get_32()

	return model_result
