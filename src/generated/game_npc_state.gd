# THIS FILE IS AUTOGENERATED BY A CCL TOOL. DO NOT EDIT.

@obfuscate
@obfuscate_path
class_name GameNpcState

const MODEL_ID_LUMENFALL_NPC_STATE = 4

var schema_version: int
var npc_id: int
var mood: int
var behavior: int
var trust_player: float
var fear: float
var hope: float
var position_x: float
var position_y: float
var position_z: float
var important_event_ids: Array[int]
var relationship_npc_ids: Array[int]
var relationship_values: Array[float]
var choice_ids: Array[int]
var has_position: bool

func get_model_id() -> int:
	return MODEL_ID_LUMENFALL_NPC_STATE

func clone_empty() -> GameNpcState:
	return GameNpcState.new()


func deep_clone() -> GameNpcState:
	var clone := GameNpcState.new()
	clone.schema_version = schema_version
	clone.npc_id = npc_id
	clone.mood = mood
	clone.behavior = behavior
	clone.trust_player = trust_player
	clone.fear = fear
	clone.hope = hope
	clone.position_x = position_x
	clone.position_y = position_y
	clone.position_z = position_z
	clone.important_event_ids = important_event_ids.duplicate()
	clone.relationship_npc_ids = relationship_npc_ids.duplicate()
	clone.relationship_values = relationship_values.duplicate()
	clone.choice_ids = choice_ids.duplicate()
	clone.has_position = has_position
	return clone

func serialize_binary() -> PackedByteArray:
	var buffer := StreamPeerBuffer.new()
	buffer.big_endian = false

	buffer.put_32(self.schema_version)

	buffer.put_32(self.npc_id)

	buffer.put_32(self.mood)

	buffer.put_32(self.behavior)

	buffer.put_float(self.trust_player)

	buffer.put_float(self.fear)

	buffer.put_float(self.hope)

	buffer.put_float(self.position_x)

	buffer.put_float(self.position_y)

	buffer.put_float(self.position_z)

	buffer.put_u32(self.important_event_ids.size())
	for item in self.important_event_ids:
		buffer.put_32(item)

	buffer.put_u32(self.relationship_npc_ids.size())
	for item in self.relationship_npc_ids:
		buffer.put_32(item)

	buffer.put_u32(self.relationship_values.size())
	for item in self.relationship_values:
		buffer.put_float(item)

	buffer.put_u32(self.choice_ids.size())
	for item in self.choice_ids:
		buffer.put_32(item)

	buffer.put_8(1 if self.has_position else 0)

	return buffer.data_array

static func deserialize_binary(data: PackedByteArray) -> GameNpcState:
	var model_result := GameNpcState.new()
	if not data or data.is_empty():
		return model_result

	var buffer := StreamPeerBuffer.new()
	buffer.big_endian = false
	buffer.data_array = data

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.schema_version = buffer.get_32()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.npc_id = buffer.get_32()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.mood = buffer.get_32()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.behavior = buffer.get_32()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.trust_player = buffer.get_float()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.fear = buffer.get_float()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.hope = buffer.get_float()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.position_x = buffer.get_float()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.position_y = buffer.get_float()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	model_result.position_z = buffer.get_float()

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	var important_event_ids_len := buffer.get_u32()
	model_result.important_event_ids = [] as Array[int]
	for i in range(important_event_ids_len):
		if buffer.get_size() - buffer.get_position() < 4:
			return model_result
		model_result.important_event_ids.append(buffer.get_32())

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	var relationship_npc_ids_len := buffer.get_u32()
	model_result.relationship_npc_ids = [] as Array[int]
	for i in range(relationship_npc_ids_len):
		if buffer.get_size() - buffer.get_position() < 4:
			return model_result
		model_result.relationship_npc_ids.append(buffer.get_32())

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	var relationship_values_len := buffer.get_u32()
	model_result.relationship_values = [] as Array[float]
	for i in range(relationship_values_len):
		if buffer.get_size() - buffer.get_position() < 4:
			return model_result
		model_result.relationship_values.append(buffer.get_float())

	if buffer.get_size() - buffer.get_position() < 4:
		return model_result
	var choice_ids_len := buffer.get_u32()
	model_result.choice_ids = [] as Array[int]
	for i in range(choice_ids_len):
		if buffer.get_size() - buffer.get_position() < 4:
			return model_result
		model_result.choice_ids.append(buffer.get_32())

	if buffer.get_size() - buffer.get_position() < 1:
		return model_result
	model_result.has_position = buffer.get_8() != 0

	return model_result
